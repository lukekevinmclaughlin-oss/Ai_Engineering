import {it,expect} from 'vitest';import {highlight,codeEditor} from '../src/editor';
it('escapes HTML and script-like source instead of producing executable nodes',()=>{const value='</textarea><img src=x onerror="alert(1)"><script>bad()</script>';const output=codeEditor('editor',value,'html');expect(output.match(/<textarea\b/g)).toHaveLength(1);expect(output.match(/<\/textarea>/g)).toHaveLength(1);expect(output).not.toMatch(/<img|<script/);expect(output).toContain('&lt;');});
it('preserves Unicode, tabs, newlines and whitespace through colouring',()=>{const code='def Grüße():\n\treturn "日本語 <&>"\n';const restored=highlight(code,'python').replace(/<\/?span[^>]*>/g,'').replaceAll('&lt;','<').replaceAll('&gt;','>').replaceAll('&quot;','"').replaceAll('&#39;',"'").replaceAll('&amp;','&');expect(restored).toBe(code);});
it('recognizes SQL comments, numbers and keywords',()=>{const output=highlight('SELECT 42 -- boundary\nFROM records','sql');expect(output).toContain('syntax-comment');expect(output).toContain('syntax-number');expect(output).toContain('syntax-keyword');});
it('recognizes strings before interpreting their comment markers',()=>expect(highlight('const url = "https://example.test/#x";','typescript')).toContain('<span class="syntax-string">&quot;https://example.test/#x&quot;</span>'));
it('keeps the duplicate visual layer out of the accessibility tree',()=>{const output=codeEditor('code','return 1','python');expect(output).toContain('<pre aria-hidden="true">');expect(output).toContain('<textarea id="code"');});
it('handles a maximum-sized unfinished string without dropping text',()=>{const value='"'+'x'.repeat(99999);expect(highlight(value).replace(/<\/?span[^>]*>/g,'').replaceAll('&quot;','"')).toBe(value);});
